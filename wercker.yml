box: ubuntu
build:
  docker: true
  steps:
    - script:
      name: testing
      code: | 
        cat /etc/resolv.conf 
        ping google.com
    - internal/docker-build:
      dockerfile: Dockerfile
      image-name: test-docker
      no-cache: true
