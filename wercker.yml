box: ubuntu
build:
  docker: true
  steps:
    - script:
      name: testing
      code: | 
        cat /etc/resolv.conf 
        nmcli dev show | grep DNS
    - internal/docker-build:
      dockerfile: Dockerfile
      image-name: test-docker
      no-cache: true
