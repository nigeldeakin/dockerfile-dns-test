box: ubuntu
build:
  steps:
    - script:
      name: testing
      code: | 
        cat /etc/resolv.conf 
    - internal/docker-build:
      dockerfile: Dockerfile
      image-name: test-docker
      no-cache: true
